# RAG Chatbot - Python dependencies (Python 3.11-3.13; developed on 3.13.2)
#
# Versions are pinned to an exact set captured from a clean resolve into a fresh
# .venv. The LangChain stack is held on the 0.3.x line so the APIs documented in
# Implementation plan/TASK_LIST.md (create_history_aware_retriever,
# create_retrieval_chain, create_stuff_documents_chain from langchain.chains)
# work as written in Phase 3.
#
# Install:  python -m venv .venv && .\.venv\Scripts\Activate.ps1
#           pip install -r requirements.txt
# Note: first `sentence-transformers` use downloads the ~130 MB bge model once.

# --- LangChain orchestration (0.3.x line) ---
langchain==0.3.30
langchain-core==0.3.86
langchain-community==0.3.31
langchain-text-splitters==0.3.11
langchain-chroma==0.2.6
langchain-nvidia-ai-endpoints==0.3.19

# --- Embeddings & vector store ---
sentence-transformers==5.5.1
chromadb==1.5.9

# --- PDF parsing (PyMuPDF imports as `fitz`) ---
pdfplumber==0.11.9
PyMuPDF==1.27.2.3

# --- Web API (uvicorn[standard] pulls watchfiles/httptools on Windows) ---
fastapi==0.136.3
uvicorn[standard]==0.49.0

# --- Config & validation ---
python-dotenv==1.2.2
pydantic==2.13.4
